# Teams but (actually) good API

This is a small API that is mainly made to be able to sync users plugin data to MongoDB to be able to retrive them on another browser or device

The login is made with Discord OAuth2
